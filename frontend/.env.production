# Set this to the deployed backend's public URL before building for production.
# Filled in on deploy day (see plan Day 2/9) — placeholder until then.
VITE_API_URL=https://your-backend-url.example.com
